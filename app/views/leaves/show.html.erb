
<div class="container-fluid bg-1 text-center">   
<div class= "container">
   <h1><%= @leave.user.firstname %></h1>
<table border='1'>
   <thead><tr>  
    <th>Leave Form</th>
    <th>Leave Details</th>
  </tr></thead>
  <tboby>
    <tr><td>From:</td> <td><%= @leave.from_date.strftime("%d-%m-%Y") %></td></tr>
    <tr><td>To:</td> <td><%= @leave.to_date.strftime("%d-%m-%Y") %></td></tr>
    <tr><td>Type of Leave:</td> <td><%= @leave.type_of_leave %></td></tr>
    <tr><td>Reason:</td> <td><%= @leave.reason %></td></tr>
    <tr><td>No Of Days:</td> <td><%= @leave.no_of_days %></td></tr>
  </tboby>
</table>

	<% if (current_user.firstname == @leave.user.firstname) %>
    <%= link_to 'Applied Leave', leaves_path %>
    <% end %>
</div>
</div>

